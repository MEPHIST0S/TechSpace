CREATE DATABASE COMPANY

CREATE TABLE ORDERS (
 ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
 ORDER_NUMEBER VARCHAR(255) NOT NULL UNIQUE,
 ORDER_DATE DATE NOT NULL,
 TOTAL_AMOUNT DECIMAL(10, 2) NOT NULL,
 IS_COMPLETED BOOLEAN NOT NULL DEFAULT FALSE,
 CURRENCY VARCHAR(3) NOT NULL DEFAULT 'AZN',
 IS_APPROVED BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE TABLE EMPLOYEE (
 ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
 FIRST_NAME VARCHAR(255) NOT NULL,
 LAST_NAME VARCHAR(255) NOT NULL,
 DATE_OF_BIRTH DATE NOT NULL,
 EMAIL VARCHAR(255) NOT NULL,
 SALARY DECIMAL(10, 2) NOT NULL,
 DEPARTMENT VARCHAR(255),
 IS_MANAGER BOOLEAN NOT NULL DEFAULT FALSE
);

ALTER TABLE ORDERS
CHANGE COLUMN ORDER_NUMEBER ORDER_NUMBER VARCHAR(255) NOT NULL UNIQUE;

INSERT INTO ORDERS (ORDER_NUMBER, ORDER_DATE, TOTAL_AMOUNT, IS_COMPLETED, CURRENCY, IS_APPROVED)
VALUES
('001', '2024-08-09', 150.75, FALSE, 'AZN', TRUE),
('002', '2024-08-10', 200.20, TRUE, 'USD', FALSE);